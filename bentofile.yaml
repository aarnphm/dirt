service: "service.py:svc"
labels:
  owner: bentoml-team
  project: gallery
include:
- "*.py"
exclude:
- tests
- "*.go"
python:
  packages:
    - scikit-learn
    - pandas
    - awscli
  lock_packages: False
docker:
  distro: debian
  system_packages:
    - nginx
  dockerfile_template: ./Dockerfile-aws.template
